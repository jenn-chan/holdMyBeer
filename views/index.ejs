<%- include("partials/header") %>

<h1>HOME</h1>

<% beers.forEach(function(beer) { %>
    <div>
        <h3><%= beer.name %></h3>
        <img src='<%= beer.image %>'> 
    </div>
<% }); %>

<%- include("partials/footer") %>